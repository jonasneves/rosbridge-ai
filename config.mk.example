# Copy this file to config.mk and fill in your values.
# config.mk is gitignored — your credentials will not be committed.

WIFI_SSID = your_wifi_ssid
WIFI_PASS = your_wifi_password
# Both PORT and MQTT_IP are auto-detected. Override here only if needed.
# PORT    = /dev/cu.usbserial-XXXXXXXX  # auto-detected from /dev/cu.usbserial-*
# MQTT_IP = 192.168.1.1                 # auto-detected via ipconfig getifaddr en0
ESP32_IP  =                             # ESP32 IP for OTA — shown in Serial Monitor after boot
